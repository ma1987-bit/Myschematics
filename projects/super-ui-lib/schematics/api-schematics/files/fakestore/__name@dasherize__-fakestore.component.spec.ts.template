import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { FormsModule } from '@angular/forms';
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { RouterTestingModule } from '@angular/router/testing';
import { <%=classify(name)%>Fakestore } from './<%=dasherize(name)%>-fakestore.component';
import { <%=classify(name)%>Service } from './<%=dasherize(name)%>.service';

describe('<%=classify(name)%>Fakestore', () => {
  let component: <%=classify(name)%>Fakestore;
  let fixture: ComponentFixture<<%=classify(name)%>Fakestore>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [<%=classify(name)%>Fakestore],
      imports: [FormsModule, HttpClientTestingModule, RouterTestingModule],
      providers: [<%=classify(name)%>Service]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(<%=classify(name)%>Fakestore);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
